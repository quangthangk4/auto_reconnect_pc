chạy file start_wifi_slient để CHẠY ngầm auto reconnect đến inet-free wifi
chạy check_status để XEM start_wifi_slient có đang chạy không
chạy stop_wifi để TẮT start_wifi_slient chạy ngầm

===============================
flow: mở máy -> chạy file start_wifi_slient -> chạy check_status để xem chạy thành công chưa -> mở wifi chọn đúng wifi -> enjoy! -> (nếu muốn tắt auto chạy stop_wifi)